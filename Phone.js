/**
 * Creating classes:
 *
 * Class declaration: class Name {}
 * Class expression:  const Name = class {}
 */

class Phone {
  constructor(
    // Defines parameters:
    color,
    length,
    width,
    
  ) {
    // Define properties:

    this.color = color;
    this.length = length;
    this.width = width;
  }
}

export default Phone;